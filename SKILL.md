---
name: ai-tell-free-web
description: 웹 화면(랜딩·대시보드·어드민·보고서)과 한국어 글(칼럼·보고서·알림·상세페이지 문안)에서 AI가 뽑은 티를 없애는 규칙. 화면·목업·UI·랜딩·대시보드를 새로 그리기 전, 화면 문구나 카드 제목을 지을 때, 글을 사람이 쓴 것처럼 고쳐 달라고 할 때("AI 티 없애줘", "사람이 쓴 것처럼", "휴머나이즈"), "AI 티 난다·밋밋하다·다 비슷하다"는 지적을 받았을 때 쓴다.
disable-model-invocation: false
---

# AI 티 없는 웹 화면과 글

지시 없이 화면을 뽑으면 항상 같은 모양이 나온다. 크림 바탕에 흰 카드, 진한 테두리,
카드마다 왼쪽 색 세로바, 브랜드색으로 칠한 제목, 이모지 알약 배지, 그리고
"어디서 나갔나" 같은 물음꼴 카드 제목. 글도 마찬가지다. "단순한 도구가 아니라",
"핵심은", "도움이 되셨길 바랍니다"가 어디서나 나온다. 이 묶음은 그 기본값을 끊는다.

두 층이 있다. **화면**은 아래 순서대로, **글**은 맨 아래 "글을 넣으면 고쳐 쓴다"대로.

## 코드를 쓰기 전에 하는 것

**1. 톤을 하나 정하고 한 줄로 선언한다.**
`references/DESIGN-COPY.md` 12.1의 톤 후보 9종에서 하나를 고른다. 사용자에게 묻지 않고
프로젝트 맥락(브랜드·타겟·용도)을 보고 정한 뒤 선언한다. 섞지 않는다.

```
톤: 에디토리얼 매거진, 90% 흑백 + 형광 옐로 액센트 10%
```

**2. 판(뼈대)을 고른다.**
`references/SKELETONS.md` 의 표에서 화면 성격에 맞는 뼈대 1개를 고른다.
고민되면 `sk-report`. **`references/SKELETONS.html` 을 브라우저로 열어 눈으로 확인한다.**
글로 읽고 해석하지 말고 파일을 연다. 고른 뼈대의 CSS 블록과 마크업을 그대로 떼어 쓴다.
한 화면에 두 뼈대를 섞지 않는다.

**3. 시각 규칙 9개를 읽는다.**
`references/VISUAL-RULES.md` 상단의 요약 9줄을 그대로 작업 지시에 넣는다.
뼈대가 인상을 정한다면 이 9줄이 품질을 정한다. 둘 다 필요하다.

## 글자를 쓸 때

`references/WRITING.md` 의 "걸리면 고칠 것" 표를 본다. 긴 줄표, 줄 머리 기호, 이모지,
혁신적·차별화된, 결론적으로·정리하자면, "지금 바로 시작하세요", 그리고 안내문에 남은
대화창 말투("도움이 되셨길", "좋은 질문")와 랜딩 카피의 반박 틀("단순한 X가 아닙니다") 같은 것들이다.

카드·표·그래프 제목은 `references/CARD-TITLES.md` 대로 **두 어절 명사구**로 짓는다.
물음꼴("어디서 나갔나")은 쓰지 않고 짝 표에서 바꿔 쓴다.
왼쪽 메뉴에 같은 이름의 화면이 있으면 그 이름을 그대로 쓴다.

## 만들고 나서

**자가 점검 7개를 대조한다.** `references/VISUAL-RULES.md` 맨 아래에 있다.
마지막으로 한 번 더 묻는다. **"숫자와 문장을 다 지워도 화면이 똑같아 보이는가."**
똑같아 보이면 껍데기만 만든 것이다.

**문구는 기계로 훑는다.**

```bash
python3 tools/screen_copy.py <프로젝트폴더>
python3 tools/screen_copy.py <프로젝트폴더> --strict   # 배포 직전
```

종료 코드는 0 깨끗, 2 볼 것만 있음, 3 고칠 것 있음이다. 배포 스크립트에 그대로 물린다.
평소에는 경고로 두고 배포 직전에만 `--strict` 로 막는다. 옛 프로젝트는 쌓인 게 많아
처음부터 막으면 배포가 통째로 멈춘다.

## 글을 넣으면 고쳐 쓴다

사용자가 글을 붙여 넣거나 파일을 가리키며 AI 티를 없애 달라고 하면 `references/HUMANIZE-KO.md`
를 읽고 그대로 한다. 독립 스킬로는 https://github.com/wnstjd878/humanizer-ko 가 같은 내용이다. [blader/humanizer](https://github.com/blader/humanizer) 의 절차를 한국어에
맞춘 것이다. 요지는 넷이다.

1. **패턴을 찾는다.** 35패턴과 대조한다. 먼저 `tools/humanize_check.py` 로 기계가 잡는 것부터 본다.
2. **주장은 다 남기고 사실은 지어내지 않는다.** 이름·숫자·날짜·인용은 원문이나 사용자한테서만.
   빈 자리는 물어본다.
3. **목소리를 맞춘다.** 사용자가 자기 글 샘플을 주면 그 버릇이 규칙보다 우선한다. 성격은 칼럼·에세이에만.
4. **두 번 쓴다.** 초안 → "아직 AI 같은 데가 어디인가, 사실이 더해지거나 빠졌는가" → 최종본.
   붙여 넣은 글이면 초안·남은 패턴·최종본을, 파일이면 최종본만 파일에, 다른 작업 안에서면 최종본만.

```bash
python3 tools/humanize_check.py 글.md            # 줄 번호와 패턴 번호를 준다
python3 tools/humanize_check.py - < 글.txt       # 표준입력
```

한국어 문법 층(번역투·이중 피동·대구 반복·연결어미 뒤 쉼표)은 여기서 규칙을 새로 만들지 말고
im-not-ai(MIT)를 읽어 쓴다. https://github.com/epoko77-ai/im-not-ai
HUMANIZE-KO.md 가 겹치는 자리마다 그쪽 번호를 적어 두었다.

## 파일 지도

| 파일 | 언제 |
|---|---|
| `references/DESIGN-COPY.md` | 톤을 정할 때. 금지 폰트·레이아웃·어휘의 원본 |
| `references/SKELETONS.md` + `.html` | 판을 고를 때. **HTML을 브라우저로 연다** |
| `references/VISUAL-RULES.md` | 그리기 전과 그린 후 |
| `references/WRITING.md` | 화면 글자를 쓸 때 |
| `references/CARD-TITLES.md` | 카드·표·그래프 제목을 지을 때 |
| `references/HUMANIZE-KO.md` | 글을 사람이 쓴 것처럼 고칠 때. 35패턴과 절차 |
| `tools/screen_copy.py` | 화면을 다 만든 뒤, 그리고 배포 직전 (소스 코드 안의 문구) |
| `tools/humanize_check.py` | 글을 고치기 전과 후 (.md .txt, 표준입력) |

## 두 층을 섞지 않는다

시각 규칙(`VISUAL-RULES.md`)은 **품질**을, 뼈대(`SKELETONS.md`)는 **인상**을 담당한다.
9규칙만 지키면 AI 티는 안 나지만 모든 화면이 똑같이 생기게 된다. 그래서 뼈대를 여러 개
두고 돌려쓴다. 뼈대를 늘릴 때 9규칙을 건드리지 않는다.

글도 같다. `HUMANIZE-KO.md` 는 **내용과 태도**(부풀림·홍보·반박 틀·대화창 잔여)를,
im-not-ai 는 **한국어 문법**(번역투·피동·어미)을 담당한다. 한쪽에 다른 쪽 규칙을 베껴 넣지 않는다.
